# 0002. 用例标签采用全局与评测对象两级作用域

日期：2026-08-03

## 决策

用例标签只支持两种不可变作用域：`global` 和 `target`。全局标签可用于所有评测对象；对象专属标签只可用于所属评测对象。评测场景仅用于用例归类，不再拥有或限制用例标签。

## 背景与取舍

评测对象下的场景通常并不完整，导入用例时也常常只有原始输入。让标签依赖场景会阻塞未归类用例，并使跨场景复用变得脆弱。将边界提升到评测对象后，标签仍有业务归属，同时允许后续人工或模型补充分场景归类。

问题标签（Badcase 的 `Issue Tag`）维持独立的全局目录，不受此决策影响。
